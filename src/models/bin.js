class Bin {
    constructor(
        id,
        timestamp,
        capacity,
        trashbin
    ) {
        this.id = id
        this.timestamp = timestamp
        this.capacity = capacity
        this.trashbin = trashbin
    }
}

module.exports = Bin