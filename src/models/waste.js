class Waste {
    constructor(
        id,
        hazwaste,
        recyclable,
        residual
    ) {
        this.id = id
        this.hazwaste = hazwaste,
        this.recyclable = recyclable,
        this.residual = residual
    }
    
}

module.exports = Waste